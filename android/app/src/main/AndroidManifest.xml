<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <!-- Keeps the link flow's Dart polling out of Doze / App Standby while
         a bank link is in progress (and through the first runSync). See
         LinkSyncForegroundService for the why. `dataSync` is the correct
         foreground-service type for sync-driven network work and is what
         Android 14+ requires the per-type permission for. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <!-- Deterministic app-side dwell timers (DwellAlarms). Denied by default
         on Android 14+; the reliability prompt asks the user to grant it, and
         DwellAlarms degrades to inexact alarms when it isn't. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <!-- Lets the reliability prompt show the system "ignore battery
         optimizations" dialog — OEM battery managers killing receivers is
         the main real-world cause of missed store notifications. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <receiver
            android:name=".MerchantGeofenceReceiver"
            android:exported="false"/>
        <receiver
            android:name=".BoundaryGeofenceReceiver"
            android:exported="false"/>
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="false">
            <intent-filter>
                <!-- Only BOOT_COMPLETED: the receiver is directBootAware=false and
                     its work needs the credential-protected DB (unavailable in
                     direct-boot), so LOCKED_BOOT_COMPLETED could never be
                     delivered nor acted on. -->
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <receiver
            android:name=".ActivityTransitionReceiver"
            android:exported="false"/>
        <receiver
            android:name=".DwellCheckReceiver"
            android:exported="false"/>
        <receiver
            android:name=".ReregisterFallbackReceiver"
            android:exported="false"/>

        <service
            android:name=".LinkSyncForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="true"/>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
